{
  "name": "unknown AG_HOOK name falls through to allow",
  "env": { "AG_HOOK": "does-not-exist" },
  "stdin": { "toolCall": { "name": "run_command", "args": { "CommandLine": "rm -rf /" } } },
  "expect_exit": 0,
  "expect_stdout_contains": ["\"decision\":\"allow\""]
}
